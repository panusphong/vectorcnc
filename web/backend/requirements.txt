fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
opencv-python-headless>=4.8
numpy>=1.24
scipy>=1.10
shapely>=2.0
ezdxf>=1.1
vtracer>=0.6.0
svgpathtools>=1.6
scikit-image>=0.22
anthropic>=0.40
pillow>=10.0
potracer>=0.0.4
pymupdf>=1.24
cairosvg>=2.7
psd-tools>=1.9
mapbox-earcut>=1.0.1
fonttools>=4.50
uharfbuzz>=0.39
pytesseract>=0.3.10
pyautotrace>=0.0.7
# ✂️ ตัดพื้นหลัง (พี่สั่ง 2026-08-18)
#    rembg ใช้ onnxruntime รันโมเดลแยกวัตถุบน CPU — ไม่ต้องมี GPU
#    ⚠️ ตัวโมเดล (isnet-general-use ~179 MB) ไม่ได้มากับแพ็กเกจ มันโหลดครั้งแรกที่ใช้
#       แล้วเก็บไว้ที่ /root/.rembg — ครั้งแรกหลัง deploy จะช้ากว่าปกติ ~10 วิ
#       ตั้ง CUTOUT_MODEL=u2netp ได้ถ้าอยากได้โมเดลเล็ก (4.7 MB) แลกกับคุณภาพ
#    🛟 ถ้าติดตั้งไม่ได้ ระบบยังตัดพื้นหลังแบบอัลกอริทึมได้ตามปกติ (ไม่พัง)
rembg>=2.0.50
onnxruntime>=1.17
